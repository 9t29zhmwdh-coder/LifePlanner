{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core IPC plus the file picker for ICS calendars.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
